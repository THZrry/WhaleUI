# 第一步：搭建环境
本机已有MinGW64和git，请使用xmake和git正确配置项目以及编译选项。
渲染：SDL3, SDL_Image, SDL_Font, stb_image, stb_image_write, stb_font等，音频和视频流出位置但注释，暂不考虑。
布局：lexbor
字符：Charset等
任务：其他相关库等
其他自行补充，要求C/C++14实现，最小依赖且兼顾性能与空间占用。

stb_font不支持彩色Emoji，参考 https://github.com/nothings/stb/issues/512 与 https://github.com/nothings/stb/pull/1135 的支持并作为补充在项目中实现。

Build Target比较多，包括：
- **Full**：全功能包含，使用SDL的图片字体等实现（不集成stb）
- **Lite**：全功能的精简实现，砍掉未使用的功能，使用stb管理资源
- **Minimal**：只保留布局相关的功能，不支持缓存等功能，保留stb管理资源，用于仅拿来做简单界面的极限场景。因此，css渲染相关的内容需要大幅度裁剪，三者的功能取舍在rules/target.md

# 第二步：内部文档 + 第三步：确定定义
先在项目下doc文件夹中保留markdown格式的文档，先把项目整体的文件结构定下来。编写底层文档，把主要的类、结构体、函数和对应的功能明确下来。

然后编写使用文档，把用例、函数确定下来。这部分主要包括应用API（主题、颜色、渲染选项等），窗口API（底层窗口内容），DOM API（与javascript保持对应），CSS API等。

再编写对应的include文件，宏定义完成后，先做好暴露API的定义（可以以C包装调用C++函数），再定义内部接口。

最后为所有外部和内部功能写好完整的单元测试，用于检测。

# 第四步：内部实现
这是最重要的一步，需要多个子步骤完成。

- 1. DOM功能实现：接入lexbor，完成HTML、CSS文件的读取解析、DOM的生成修改
- 2. 标签功能：由于项目是将所有tag视为div并对应特殊处理，可能需要内部表示，用于处理这个。如果有内部用于渲染的表示，为了性能，可以考虑ECS等性能优先架构。
- 3. 样式处理：为渲染做准备，处理样式
- 4. 渲染实现：经过优化的渲染实现，初步实现功能，因为HTML/CSS的标准体系过于庞大，可以参考`doc/internal/css-priority.md`先实现部分
- 5. 流程处理：整个流程可能涉及多线程和异步等。这一步用于优化流程，打磨整个loop的逻辑
- 6. 外围功能：包括应用API和窗口API等
- 7. 样式：把Chromium，多种Windows，MacOS，Material Design等样式转换成本项目使用的样式，注意需要支持热切换。
- 8. 测试：优化修改测试，直到单元测试全部通过。

# 第五步：文档
文档是必不可少的，第二步定下的文档只是API手册，这里请补充用例和具体内容。

更新README.md，按照开源项目的要求完善。我的主页是github.com/THZrry，不过作者信息和Contribute、Sponsor并不需要写上去，最主要还是项目介绍和构建指南。

还要翻译一份英文的README和文档。

# 第六步：维护
实现更多的功能，修改已有的BUG，这一步不展开详情。